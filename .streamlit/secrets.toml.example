# Copy this file to `.streamlit/secrets.toml` and populate it with your credentials.
# These values will be loaded automatically when running `streamlit run app.py`.

MISTRAL_API_KEY = "your-mistral-api-key"
# Optional: override the default hosted endpoint.
# MISTRAL_ENDPOINT = "https://api.mistral.ai/v1/chat/completions"
